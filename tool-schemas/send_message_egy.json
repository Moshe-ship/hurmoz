{
  "name": "send_message_egy",
  "description": "Send an Egyptian-Arabic-preserving message via WhatsApp, SMS, Telegram, or email. Use this variant when the calling context is bound to Egyptian dialect and the receipt should record dialect_expected=egy.",
  "parameters": {
    "type": "object",
    "properties": {
      "recipient": {
        "type": "string",
        "description": "Recipient name or phone/email.",
        "x-mtg": {
          "slot_type": "named_entity",
          "script": "mixed",
          "transliteration_allowed": true,
          "mode": "advisory"
        }
      },
      "platform": {
        "type": "string",
        "enum": ["whatsapp", "sms", "telegram", "email"],
        "description": "Messaging platform.",
        "x-mtg": {
          "slot_type": "identifier",
          "script": "latn",
          "mode": "advisory"
        }
      },
      "message": {
        "type": "string",
        "description": "Message body in Egyptian Arabic register. Must preserve the user's dialect and script.",
        "x-mtg": {
          "slot_type": "free_text",
          "script": "ar",
          "dialect_expected": "egy",
          "dialect_enforcement": "preserve",
          "transliteration_allowed": false,
          "mode": "advisory",
          "post_call_contract": ["script_match", "dialect_preserve", "no_surface_corruption"]
        }
      }
    },
    "required": ["recipient", "platform", "message"]
  }
}
